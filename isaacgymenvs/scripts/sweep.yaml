project: isaacgymenvs
name: baseline_individual
program: train_mappo.py

method: grid

metric:
  name: eval/success
  goal: maximize

parameters:

  wandb_name:
    values:
      - baseline

command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}